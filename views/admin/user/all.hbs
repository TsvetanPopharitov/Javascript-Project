<div class="container body-content">
    <div class="well"><h2>Всички потребители</h2>
        <div class="row">
            <table class="table table-striped table-hover ">
                <thead>
                <tr>
                    <th># Хаш</th>
                    <th>Имена</th>
                    <th>Имейл</th>
                    <th>Действие</th>
                </tr>
                </thead>
                <tbody> {{#each users}}
                    <tr {{#if this.isAdmin }}class="info" {{/if}}>
                        <td>{{ this.id }}</td>
                        <td>{{ this.fullName }}</td>
                        <td>{{ this.email }}</td>
                        <td><a href="/admin/user/edit/{{this.id}}">Редакция</a> <a href="/admin/user/delete/{{this.id}}">Изтриване</a>
                        </td>
                    </tr> {{/each}} </tbody>
            </table>
        </div>
    </div>
</div>